export {
  DEFAULT_GESTURES,
  fist,
  ok,
  openHand,
  peace,
  thumbsUp,
} from "./builtinGestures";
export { default as detectGesture } from "./detectGesture";

export type { GestureDetector, GestureResult } from "./types";
