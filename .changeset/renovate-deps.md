---
"@omnidotdev/rdk": patch
---

Upgrade dependencies: `three` and `@types/three` to `^0.185.0` (bumps the `three` peer range), plus dev tooling `vite-plugin-dts` to v5 and `vite-plugin-mkcert` to v2.
