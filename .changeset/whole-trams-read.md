---
"@omnidotdev/rdk": minor
---

**Breaking:** Changed geolocation module anchor `onAttached` to `onAttach` and `onGpsUpdated` to `onGpsUpdate`. This normalizes the callbacks to present tense, following the React convention.
